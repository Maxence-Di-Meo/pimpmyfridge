{
  "scripts": {
    "backend:test": "npm --prefix backend test",
    "frontend:build": "npm --prefix frontend/pimp-my-fridge run build",
    "frontend:test": "npm --prefix frontend/pimp-my-fridge test -- --watchAll=false"
  },
  "dependencies": {
    "axios": "^1.7.9",
    "knex": "^3.1.0",
    "recharts": "^2.15.0"
  }
}
